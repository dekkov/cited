## What

<!-- 1-2 sentences: what does this PR do? -->

## Why

<!-- Link to issue or requirement REQ-ID -->

## Tests

<!-- What tests were added or run? -->

## Checklist

- [ ] DCO sign-off on every commit (`git commit -s`)
- [ ] Conventional commit messages (`feat:`, `fix:`, `docs:`, etc.)
- [ ] `pnpm lint && pnpm typecheck && pnpm test` passes
- [ ] If this touches clip text or `risk_flags`: `needs-medical-review` label applied
- [ ] If this touches `docs/legal/` or `privacy-policy.md`: legal-review label applied
